###############################
#[backend]
###############################
# Copy to `.env.local` (gitignored). Group headers are required so env.split
# can bake these vars into `.output/env.compose.backend` for Docker Compose.

OPENAI_API_KEY=
ANTHROPIC_API_KEY=
